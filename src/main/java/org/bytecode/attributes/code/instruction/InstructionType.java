package org.bytecode.attributes.code.instruction;

public enum InstructionType {
    LOAD,
    STORE,
    STACKBEHAVIOR,
    CALCULATE,
    LOGICAL,
    LOCALBEHAVIOR,
    CONVERT,
    SWITCH,
    JUMP,
    COMPARE,
    IF,
    RETURN_INSC,
    FIELDBEHAVIOR,
    INVOKE,
    NEW_INSC,
    LOCK,
}
